import React from 'react';
import Layout from '../../components/Layout'

const Home = (props) => {
    return(
        <Layout>
            <h1 style={{margin: '5rem',}} className="text-center" >Welcome To Admin Dashboard</h1>
        </Layout>
    )
}

export default Home